# dotfiles conf for meetup specific stuff
# run after devbox or home setup

- clean: ['~']

- link:
    ~/.gitconfig: meetup/gitconfig
    ~/.mupweb.secrets: meetup/mupweb.secrets
    ~/.mupweb.config: meetup/mupweb.config


